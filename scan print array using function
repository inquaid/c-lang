#include<stdio.h>
#define n 5
void scan(int *ary, int a) {
scanf("%d", &ary[a]);
}
void print(int *ary, int b) {
  printf("%d ", ary[b]);
}


int main() {
int ary[n];
for(int i=0; i<n;i++) {
  scan(ary,i);
}
for(int i=0; i<n;i++) {
  print(ary, i);
}

return 0;
}
