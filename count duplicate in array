#include<stdio.h>
#define N 5
#define m 5
int main() {
  int ary[N], arr[m];
  
  for(int i=0; i<N ; i++) {
    printf("element %d is ", i);
    scanf("%d", &ary[i]);

  } 
int sum=0;
  for(int j=0; j<N; j++ ) {
    for(int p=j + 1; p<N+1; p++) {
        if(ary[j]==ary[p]) sum = sum+1;
    }
  }
  printf("%d ", sum);



  return 0;
}
