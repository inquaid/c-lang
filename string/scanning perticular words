#include <stdio.h>

int main()
{
    char buffer[100];

    // Read lowercase letters into the buffer
    scanf("%[abcdefghijkpqrstuvwxyz]", buffer);
    // i can choose which word can be scaned or not by this [...]
    printf("Input: %s\n", buffer);

    return 0;
}
