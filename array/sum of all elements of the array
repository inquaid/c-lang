#include <stdio.h>
#define n 5
void scan(int *ary, int a)
{
  for (int i = 0; i < n; i++)
  {
    scanf("%d", &ary[i]);
  }
}

void print(int *ary, int b)
{
  for (int i = 0; i < n; i++)
  {
    printf("%d ", ary[i]);
  }
}

int main()
{
  int ary[n];
  scan(ary, n);
  int sum = 0;
  for (int i = 0; i < n; i++)
  {
    sum = sum + ary[i];
  }
  printf("%d", sum);
  return 0;
}
