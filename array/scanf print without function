#include <stdio.h>
#define n 5
int main()
{
  int ary[n];
  for (int i = 0; i < n; i++)
  {
    scanf("%d", &ary[i]);
  }

  for (int i = 0; i < n; i++)
  {
    printf("%d ", ary[i]);
  }

  return 0;
}
